const spMoney=new Intl.NumberFormat('nb-NO',{maximumFractionDigits:0});
const spYenMoney=new Intl.NumberFormat('nb-NO',{maximumFractionDigits:0});
let spData=null,spLoading=false;
function spN(v){return Number(v??0)}
function spKr(v){return `${spMoney.format(spN(v))} kr`}
function spY(v){return `¥${spYenMoney.format(spN(v))}`}
function spEnsure(){const anchor=document.getElementById('purchase-check');if(!anchor||document.getElementById('scenario-planner'))return!!anchor;const el=document.createElement('section');el.id='scenario-planner';el.className='scenario-planner';el.innerHTML=`<div class="sp-head"><div><p class="panel-kicker">HVA HVIS?</p><h2>Test et scenario</h2><p>Samme Japan-runway som resten av MoneyOS. Ingen data endres.</p></div></div><form id="sp-form" class="sp-form"><select id="sp-type"><option value="once">Engangsutgift</option><option value="monthly_cost">Ekstra hver måned</option><option value="monthly_save">Sparer hver måned</option></select><div class="sp-amount"><input id="sp-amount" type="number" min="1" step="1" placeholder="Beløp" required><span>kr</span></div><select id="sp-months"><option value="1">1 måned</option><option value="3">3 måneder</option><option value="5" selected>5 måneder</option></select><button type="submit">Beregn</button></form><div id="sp-result" class="sp-result hidden"></div>`;anchor.insertAdjacentElement('afterend',el);document.getElementById('sp-form')?.addEventListener('submit',e=>{e.preventDefault();spCalc()});return true}
async function spGetRunway(){for(let i=0;i<40&&!window.MoneyOSJapanRunway;i++)await new Promise(r=>setTimeout(r,100));try{return await window.MoneyOSJapanRunway?.get?.()}catch{return null}}
function spSnapshotAgeDays(snapshotDate){if(!snapshotDate)return null;const d=new Date(`${String(snapshotDate).slice(0,10)}T12:00:00`);if(Number.isNaN(d.getTime()))return null;return Math.max(0,Math.floor((Date.now()-d.getTime())/86400000))}
async function spCalc(){if(!spData)return;const type=document.getElementById('sp-type').value,amount=spN(document.getElementById('sp-amount').value),months=Math.max(1,spN(document.getElementById('sp-months').value));if(!(amount>0))return;const runway=await spGetRunway(),safe=spN(spData.overview?.safe_to_spend);let impact=0,label='';if(type==='once'){impact=-amount;label='Engangsutgift'}else if(type==='monthly_cost'){impact=-(amount*months);label='Ekstra månedlig kostnad'}else{impact=amount*months;label='Månedlig besparelse'}const japanBefore=runway?.free_nok??null,japanAfter=japanBefore==null?null:japanBefore+impact,safeAfter=type==='once'?safe-amount:safe,japanDailyAfter=japanAfter!=null&&runway?.days_remaining>0&&runway?.rate_jpy_nok>0?Math.max(0,japanAfter)/runway.rate_jpy_nok/runway.days_remaining:null,snapshotAge=spSnapshotAgeDays(runway?.account_snapshot_date);let verdict='Scenarioet holder seg innenfor den kjente Japan-planen.',tone='ok';if(japanAfter!==null&&japanAfter<0){verdict=`Scenarioet mangler ${spKr(Math.abs(japanAfter))} for å holde Japan-planen.`;tone='bad'}else if(type==='once'&&safeAfter<0){verdict=`Engangsutgiften går ${spKr(Math.abs(safeAfter))} over det som er trygt frem til lønn.`;tone='bad'}else if(runway?.missing_costs>0){verdict=`Scenarioet ser mulig ut, men ${runway.missing_costs} Japan-kostnad${runway.missing_costs===1?'':'er'} mangler fortsatt pris.`;tone='warn'}else if(snapshotAge!=null&&snapshotAge>3){verdict=`Scenarioet ser mulig ut, men kontosaldoen er ${snapshotAge} dager gammel.`;tone='warn'}else if(japanAfter!==null&&japanAfter<10000){verdict='Scenarioet går opp, men etterlater en liten fri Japan-pott.';tone='warn'}const result=document.getElementById('sp-result');result.className=`sp-result ${tone}`;result.innerHTML=`<div class="sp-result-title"><span>${label}</span><strong>${type==='monthly_save'?'+':'−'}${spKr(amount)}${type==='once'?'':` / mnd`}</strong></div><h3>${verdict}</h3><div class="sp-grid"><div><span>Effekt over ${months} mnd</span><strong>${impact>=0?'+':''}${spKr(impact)}</strong></div><div><span>Fri Japan-pott etter scenario</span><strong>${japanAfter===null?'—':spKr(Math.max(0,japanAfter))}</strong><small>${japanBefore===null?'Runway mangler data':`fra ${spKr(Math.max(0,japanBefore))}`}</small></div><div><span>Japan per dag etter scenario</span><strong>${japanDailyAfter===null?'—':spY(japanDailyAfter)}</strong><small>${runway?.days_remaining?`${runway.days_remaining} dager igjen`:'samme runway som Japan-siden'}</small></div></div><p>${type==='monthly_cost'?'Månedskostnaden simuleres for valgt antall måneder; eksakt trekkdato er ikke antatt. ':''}${runway?'Japan-beregningen inkluderer samme faste kostnader, nødvendige reserver, turer og verifiserte Cash/ICOCA som Japan-siden.':'MoneyOS kunne ikke hente Japan-runwayen, så det vises ikke et alternativt estimat.'} Framtidig inntekt antas ikke.</p>`;result.classList.remove('hidden')}
async function spLoad(){if(spLoading||!spEnsure())return;const app=document.getElementById('app');if(!app||app.classList.contains('hidden'))return;spLoading=true;try{const r=await fetch('/api/dashboard',{credentials:'same-origin',cache:'no-store'});if(r.ok)spData=await r.json()}finally{spLoading=false}}
function spBoot(){const app=document.getElementById('app');if(!app)return;const run=()=>{if(!app.classList.contains('hidden'))setTimeout(spLoad,100)};run();new MutationObserver(run).observe(app,{attributes:true,attributeFilter:['class']});document.getElementById('refresh')?.addEventListener('click',()=>setTimeout(spLoad,250));document.addEventListener('moneyos:japan-wallet-updated',()=>setTimeout(spLoad,180));document.addEventListener('moneyos:transaction-updated',()=>setTimeout(spLoad,180))}
spBoot();
