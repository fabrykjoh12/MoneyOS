function adMonth(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`}
function adOpen(category){const month=adMonth(),monthButton=[...document.querySelectorAll('#month-strip [data-month]')].find(x=>x.dataset.month===month);if(monthButton)monthButton.click();setTimeout(()=>{const categoryButton=[...document.querySelectorAll('#selected-categories [data-category]')].find(x=>{try{return decodeURIComponent(x.dataset.category||'')===category}catch{return false}});if(categoryButton){categoryButton.click();return}document.getElementById('month-explorer')?.scrollIntoView({behavior:'smooth',block:'start'})},80)}
function adBind(){document.querySelectorAll('#am-essential .am-line,#am-flex .am-line').forEach(row=>{if(row.dataset.adBound)return;const category=row.querySelector('div:first-child strong')?.textContent?.trim();if(!category)return;row.dataset.adBound='1';row.classList.add('am-clickable');row.setAttribute('role','button');row.tabIndex=0;row.setAttribute('aria-label',`Vis bokførte kjøp i ${category}`);row.addEventListener('click',()=>adOpen(category));row.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();adOpen(category)}})})}
function adBoot(){const wait=()=>{const active=document.getElementById('active-month');if(active){new MutationObserver(adBind).observe(active,{childList:true,subtree:true});adBind();return}setTimeout(wait,300)};wait()}
adBoot();