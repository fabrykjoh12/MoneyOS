function hrEnsure(){const shell=document.querySelector('[data-view-panel="today"] .home-shell');if(!shell||document.getElementById('home-runway'))return!!shell;const anchor=document.getElementById('bf-home')||document.getElementById('monthly-margin-home')||shell.querySelector('.japan-home-strip');if(!anchor)return false;const el=document.createElement('section');el.id='home-runway';el.className='hr-card';el.innerHTML='<div><p class="panel-kicker">ØKONOMISK RUNWAY</p><h2 id="hr-title">Beregner…</h2><p id="hr-copy">Hvor mange kommende minimumsmåneder dagens penger kan dekke.</p></div><button type="button">Se 6 måneder →</button>';anchor.insertAdjacentElement('afterend',el);el.querySelector('button').addEventListener('click',()=>{document.querySelector('[data-view="money"]')?.click();setTimeout(()=>document.getElementById('budget-runway')?.scrollIntoView({behavior:'smooth',block:'start'}),180)});return true}
function hrSync(){if(!hrEnsure())return;const first=document.querySelector('#rw-summary>div:first-child');if(!first)return;const value=first.querySelector('strong')?.textContent?.trim(),copy=first.querySelector('p')?.textContent?.trim();if(value)document.getElementById('hr-title').textContent=value==='—'?'Runway krever flere planer':`${value} minimum-runway`;if(copy)document.getElementById('hr-copy').textContent=copy}
function hrBoot(){const app=document.getElementById('app');const run=()=>{if(app&&!app.classList.contains('hidden'))setTimeout(hrSync,1000)};run();if(app)new MutationObserver(run).observe(app,{attributes:true,attributeFilter:['class']});const watch=()=>{const summary=document.getElementById('rw-summary');if(summary){new MutationObserver(hrSync).observe(summary,{childList:true,subtree:true,characterData:true});hrSync();return}setTimeout(watch,350)};watch();document.getElementById('refresh')?.addEventListener('click',()=>setTimeout(hrSync,1700))}
hrBoot();
