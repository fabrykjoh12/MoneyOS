const siMoney = new Intl.NumberFormat('nb-NO', { maximumFractionDigits: 0 });
const siPct = new Intl.NumberFormat('nb-NO', { maximumFractionDigits: 0 });
let siLoading = false;

function siN(v){ return Number(v ?? 0); }
function siKr(v){ return `${siMoney.format(siN(v))} kr`; }
function siY(v){ return `¥${siMoney.format(siN(v))}`; }
function siEsc(v){ return String(v ?? '').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }

const merchantRules = [
  [/\brema\b/i,'REMA 1000'],[/\bmeny\b/i,'MENY'],[/\beasypark\b/i,'EasyPark'],[/apple\.com|apple com|apple\.com\/bill/i,'Apple'],[/anthropic|claude/i,'Claude'],[/openai|chatgpt/i,'ChatGPT'],[/adobe/i,'Adobe'],[/higgsfield/i,'Higgsfield'],[/supabase/i,'Supabase'],[/google.*one/i,'Google One'],[/youtube.*premium|youtubeprem/i,'YouTube Premium'],[/paddle.*geogues|geogues/i,'Geogues'],[/circle\s*k|1-2-3|st1/i,'Drivstoff'],[/clas.?ohlson/i,'Clas Ohlson'],[/elkj[oø]p/i,'Elkjøp'],[/gina\s*tricot/i,'Gina Tricot'],[/normal\b/i,'Normal'],[/phoenix\s*sushi/i,'Phoenix Sushi'],[/la\s*baguett/i,'La Baguette'],[/telia/i,'Telia'],[/telenor/i,'Telenor']
];
function normalizeMerchant(raw){const value=String(raw??'').trim();if(!value)return value;for(const [pattern,name] of merchantRules){if(pattern.test(value))return name}return value.replace(/^visa\s+\d+\s+/i,'').replace(/^varekjøp(?: i butikk)?\s+/i,'').replace(/^ubetjent varekjøp\s+/i,'').replace(/\s+dato\s+\d{1,2}\.\d{1,2}.*$/i,'').replace(/\s{2,}/g,' ').trim()}
function normalizeVisibleMerchants(){document.querySelectorAll('.transaction-row .row-title, .category-transaction-row strong:first-child').forEach(el=>{const original=el.textContent?.trim(),normalized=normalizeMerchant(original);if(original&&normalized&&original!==normalized){el.dataset.originalMerchant=original;el.textContent=normalized;el.title=original}})}
function ensureInsights(){if(document.getElementById('smart-insights'))return true;const attention=document.querySelector('.attention-section');if(!attention)return false;const section=document.createElement('section');section.id='smart-insights';section.className='smart-insights';section.innerHTML=`<div class="si-head"><div><p class="panel-kicker">MONEYOS SER</p><h2>Hva skiller seg ut?</h2></div><span>Automatisk · kun fra data</span></div><div id="si-list" class="si-list"><div class="si-empty">Analyserer økonomien…</div></div>`;attention.insertAdjacentElement('afterend',section);return true}
function monthRows(data){return Array.isArray(data.monthly_breakdown)?data.monthly_breakdown:[]}
function makeInsights(data,runway){const insights=[],months=monthRows(data),current=months[0],previous=months[1];if(current&&previous&&siN(previous.expenses)>0){const diff=siN(current.expenses)-siN(previous.expenses),pct=Math.round(Math.abs(diff)/siN(previous.expenses)*100);if(Math.abs(diff)>=1000)insights.push({tone:diff>0?'warn':'good',eyebrow:'MÅNED MOT MÅNED',title:`Du har brukt ${siKr(Math.abs(diff))} ${diff>0?'mer':'mindre'} enn måneden før.`,copy:`Det tilsvarer ${siPct.format(pct)} % ${diff>0?'høyere':'lavere'} bokført forbruk. Trykk Penger for å se hvilke kategorier som forklarer forskjellen.`})}
  if(current?.categories){const cats=Object.entries(current.categories).map(([name,amount])=>({name,amount:siN(amount)})).sort((a,b)=>b.amount-a.amount),top=cats[0],total=siN(current.expenses);if(top&&total>0){const share=Math.round(top.amount/total*100);insights.push({tone:share>=40?'neutral':'good',eyebrow:'STØRSTE DRIVER',title:`${top.name} står for ${share} % av månedens utgifter.`,copy:`${siKr(top.amount)} er bokført i denne kategorien. Store engangskjøp kan derfor gjøre måneden dyr uten at det betyr at vanlig hverdagsforbruk har økt tilsvarende.`})}}
  const fixed=siN(data.cost_summary?.fixed_monthly_total);if(fixed>0)insights.push({tone:'neutral',eyebrow:'FASTE KOSTNADER',title:`Bekreftede faste kostnader er ${siKr(fixed)} per måned.`,copy:`Det tilsvarer omtrent ${siKr(fixed*12)} per år hvis de samme kostnadene fortsetter.`});
  const reviews=data.review_candidates??[];if(reviews.length)insights.push({tone:'warn',eyebrow:'TRENGER AVKLARING',title:`${reviews.length} mulig faste kostnader er ikke bekreftet.`,copy:'De påvirker ikke fremtidsberegningen før de er bekreftet, slik at MoneyOS ikke later som usikre trekk er sikre.'});
  if(runway){if(runway.free_nok<0)insights.push({tone:'warn',eyebrow:'JAPAN · RUNWAY',title:`Japan-planen mangler ${siKr(Math.abs(runway.free_nok))}.`,copy:'Faste og nødvendige reserver er større enn tilgjengelige midler. Ingen fri dagsgrense er trygg før planen går opp.'});else insights.push({tone:runway.missing_costs?'warn':'good',eyebrow:'JAPAN · FRI RUNWAY',title:`${siY(runway.free_jpy)} kan fordeles over resten av oppholdet.`,copy:`${siY(runway.month30_jpy)} per 30 dager · ${siY(runway.weekly_jpy)} per uke · ${siY(runway.daily_jpy)} per dag over ${runway.days_remaining} dager.${runway.missing_costs?` ${runway.missing_costs} kostnad${runway.missing_costs===1?'':'er'} mangler fortsatt pris.`:''}`})}
  return insights.slice(0,4)}
function renderInsights(data,runway){if(!ensureInsights())return;const root=document.getElementById('si-list'),items=makeInsights(data,runway);root.innerHTML=items.length?items.map(x=>`<article class="si-card ${x.tone}"><div class="si-marker"></div><div><span>${siEsc(x.eyebrow)}</span><h3>${siEsc(x.title)}</h3><p>${siEsc(x.copy)}</p></div></article>`).join(''):'<div class="si-empty">Ingen tydelige avvik å vise akkurat nå.</div>'}
async function siGetRunway(){for(let i=0;i<40&&!window.MoneyOSJapanRunway;i++)await new Promise(r=>setTimeout(r,100));try{return await window.MoneyOSJapanRunway?.get?.()}catch{return null}}
async function loadInsights(){if(siLoading)return;const app=document.getElementById('app');if(!app||app.classList.contains('hidden'))return;siLoading=true;try{const r=await fetch('/api/dashboard',{credentials:'same-origin',cache:'no-store'});if(!r.ok)return;const data=await r.json(),runway=await siGetRunway();renderInsights(data,runway);normalizeVisibleMerchants()}finally{siLoading=false}}
function bootSmartInsights(){const app=document.getElementById('app');if(!app)return;const run=()=>{if(!app.classList.contains('hidden'))loadInsights()};run();new MutationObserver(()=>normalizeVisibleMerchants()).observe(document.body,{subtree:true,childList:true});new MutationObserver(run).observe(app,{attributes:true,attributeFilter:['class']});document.getElementById('refresh')?.addEventListener('click',()=>setTimeout(loadInsights,250));document.addEventListener('moneyos:japan-wallet-updated',()=>setTimeout(loadInsights,300));document.addEventListener('moneyos:transaction-updated',()=>setTimeout(loadInsights,300))}
bootSmartInsights();
